/*
 * Deklaracja dźwięków
 * Dokumentacja: https://community.bistudio.com/wiki/Description.ext#CfgSounds
 */

class CfgSounds {
    // Pamiętaj, że GVAR(twojaKlasa) rozwiazuje się do mission_twojaKlasa
    class GVAR(intro_sound) {
        // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
        name = "A4E Mission Intro";

        // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
        sound[] = {"data\intro.ogg", 2, 1, 100};

        // Napisy wyświetlane wraz z dźwiękiem
        titles[] = {};
    };

    class GVAR(ChickenIntro) {
        // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
        name = "McChicken Intro";

        // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
        sound[] = {"data\mcchicken.ogg", 2, 1, 100};

        // Napisy wyświetlane wraz z dźwiękiem
        titles[] = {};
    };



    class GVAR(briefing_1) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 1";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-1.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = { };
    };

    class GVAR(briefing_2) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 2";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-2.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_3) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 3";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-3.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = { };
    };

    class GVAR(briefing_4) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 4";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-4.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_5) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 5";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-5.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_6) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 6";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-6.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_7) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 7";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-7.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_8) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 8";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-8.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_9) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 9";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-9.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_10) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 10";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-10.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_11) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 11";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-11.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_12) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 12";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-12.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };

    class GVAR(briefing_13) {
      // Nazwa dźwięku, nazwa techniczna, niewidoczna dla graczy, wyświetlana w edytorze (np. w efektach triggerów)
      name = "A4E Mission Briefing 13";

      // Ścieżka do pliku dźwiękowego, głośność, wysokość tonu, dystans z którego słychać dźwięk
      sound[] = { "data\sound_briefing\briefing-kwestia-13.ogg", 2, 1, 100 };

      // Napisy wyświetlane wraz z dźwiękiem
      titles[] = {  };
    };


}; // zamknięcie cfgsounds
